import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  innerContainer: {
    borderWidth: 0.3,
    borderRadius: 10,
    height: 40,
    width: 300,
    alignItems: 'center',
    marginTop: 20,
  },
  text: {
    textAlign: 'center',
    marginTop:10,
  },
});
