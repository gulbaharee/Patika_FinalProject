import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        alignItems:'center',
    },
    input:{
        borderWidth:0.3,
        borderRadius:10,
        height:40,
        width:300,
        textAlign:'center',
        marginBottom:10,
    }
})